{% extends 'base/base.html.twig' %}

{% block body %}
    <h1>Listado de jugadores</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Club</th>
            </tr>
        </thead>
        <tbody>
        {% for player in players %}
            <tr>
                <td>{{ player.id }}</td>
                <td>{{ player.name }}</td>
                <td>{{ player.club.name }}</td>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
